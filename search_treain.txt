aimport java.util.scanner;
class search
{ 
  public static void main(string args[])
  {
    scanner S=new scanner (system.in);
    system.out.println("enter the train name");
     train name=S.nextInt();
     system.out.println(" train name="+ train name);
   }
}
